<%= form_for @blogger do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %><br>

  <%= f.label :img_url %>
  <%= f.text_field :img_url %><br>

  <%= f.label :location %>
  <%= f.text_field :location %><br>

  <%= f.submit %>

<% end %>
