<h1>Recipes</h1>

  <% @recipes.each do |recipe| %>
  <div class="recshow">

      <a id="recipe" href="/recipes/<%= recipe.id %>">
        <%= recipe.name %>
      </a><br>
      <img src="<%= recipe.img_url %>">
      <%= recipe.recipe %>

    </div>
  <% end %>
